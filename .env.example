# Only used by `just plan`/`just apply` locally; CI uses Workload Identity Federation.
# gcloud auth application-default login   (creates ADC for the google provider)
ENV=dev
# Optional: multi-model council review
ANTHROPIC_API_KEY=
OPENAI_API_KEY=
GEMINI_API_KEY=
